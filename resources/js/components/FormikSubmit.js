// Standard import items
import React, {Component} from 'react';
import ReactDOM from 'react-dom';

export default class FormikSubmit  extends Component {

	constructor(props) {
		super(props);

		this.state = {
		}

		//Bindings
		this.hideFlashMessage = this.hideFlashMessage.bind(this);
	}

	render() {
		return (

		);
	}
}
